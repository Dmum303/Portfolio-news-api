const FONTS = {
  primary: 'Gloock, serif',
};

// Tried calling this in spotlight.styles.js however
// it did not work, could not see what was in background
const COLORS = {
  background: {
    primary: '#fafafa',
    lightBlack: '#282828',
  },
};

export { FONTS, COLORS };
